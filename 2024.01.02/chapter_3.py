#변수 선언 및 초기화
number = 10
#변수 시작은 숫자 불가
#공백은 허용되지 않는다
#예약어(=키워드)를 변수로 선언할 수 없다 (중요)
#변수는 식별자, 식별자란 사용자가 만들어서 쓰는 단어

#변수 값 변경
number = 20

#변수를 사용한 계산
result = number * 2
#연산자가 두 개가 사용됨.
#'='는 할당 연산자, '*'는 산술연산자

#변수 값 출력
print(result)
